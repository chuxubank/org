[project]
name = "org-scripts"
version = "0.1.0"
description = "Python dependencies for Org Babel blocks in this notes repository"
requires-python = ">=3.14"
dependencies = [
    "reportlab==4.4.4",
]

[tool.uv]
# Dependency environment only; nothing here is built or installed as a package.
package = false
